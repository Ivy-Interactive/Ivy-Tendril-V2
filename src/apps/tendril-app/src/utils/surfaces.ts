/**
 * The app drew the same card container at a handful of different `bg-card/*` opacities across
 * views, each a hand-typed copy of the others. This is `bg-card/40`, the most common of the ten
 * values in use.
 */
export const CARD_SURFACE = "rounded-box border border-border bg-card/40";
